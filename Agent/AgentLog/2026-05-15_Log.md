# 2026-05-15 Log

---

## Compact Log #1 — 코드 문제 검증 및 수정

→ [`Refactoring_RenderAPI.md §검증 요약`](Plan/Refactoring_RenderAPI.md) 참조

`46241bc` 커밋 기반 코드 검토. 발견된 문제 9건 분류 및 수정.

| 항목 | 내용 | 완료 |
|---|---|---|
| #1 (Critical) | AnimationPass가 TransparentPass 이후 실행 — Execute.cpp 패스 순서 수정 | ✅ |
| #2 (Medium) | TransparentPass depth `DontCare` → `Store` 변경 | ✅ |
| #3 (Medium) | DebugTextPass가 AnimationPass 전에 실행 — `AddDebugTextPasses` 분리 | ✅ |
| #4 | viewProj / lights CB 이중 업로드 → `isTransparentPass` 분기로 단일화 | ✅ |
| #5 | SortKey depth 정규화 매직 넘버 100.0f → `CameraData.farZ` 사용 | ✅ |
| #6a | `ITexture2DArray` / `ITextureUAV`의 `HasAlpha()` 불필요 선언 제거 | ✅ |
| #6b (Critical) | `Texture_DirectX::HasAlpha()` 항상 `false` → stbi 채널 수로 실제 판별 | ✅ |
| #7 | `AnimationRenderer::Render` 스키닝 커맨드 없을 때 조기 반환 누락 | ✅ |
| #8 | `ForwardRenderer::Render` 도달 불가 필터 조건 `\|\| cmd.skinnedMesh` 제거 | ✅ |

---

## Compact Log #2 — RHI 추상화 A / B 구현

→ [`Refactoring_RenderAPI.md §A`, `§B`](Plan/Refactoring_RenderAPI.md) 참조

| 항목 | 작업 내용 |
|---|---|
| **A** `ICommandList::CopyBufferToTexture` | 텍스처 업로드 경로의 DX12 직접 호출 추상화. `CommandList_DirectX`에 구현 추가 |
| **B** `IRenderDevice::ImmediateSubmit` | `RenderDevice_DirectX`에 Copy 큐/할당자/펜스 캐싱 후 람다 방식으로 추상화. 기존 3개 텍스처 클래스(`Texture_DirectX`, `TextureFloat_DirectX`, `Texture2DArray_DX`)에서 임시 큐 생성 코드 제거 |
| **#9** 연계 | `ImmediateSubmit` 도입으로 업로드 인프라 3중 중복 제거 완료 |

---

## Compact Log #3 — RHI 추상화 C + WorldObject 리팩토링 + 빌드 검증

→ [`Refactoring_RenderAPI.md §C`, `§WorldObject`](Plan/Refactoring_RenderAPI.md) 참조

**추상화 C — AnimationRenderer 패스 분리:**

`AddComputePasses` / `AddGraphicsPasses` 암묵적 순서 의존 제거.  
`AddComputePasses`가 `vector<SkinnedMesh*>`를 반환하고, `AddGraphicsPasses`가 파라미터로 받는 방식으로 변경. `frameUniqueMeshes` 멤버 제거.

**WorldObject 리팩토링:**

`RenderObjectDesc` 구조체 추가 → `WorldObject`가 `renderDesc` 소유.  
`WO_Cube`를 `WorldObject` 직접 상속에서 `WorldModel` 상속으로 전환.  
Application이 `scene.AddMesh` 직접 호출하던 패턴 제거 → `cube->renderDesc.layer = 1` 선언만으로 완결.

**빌드 및 실행 검증 완료** (unstaged 상태로 유지 중)

---

## Compact Log #4 — DeferredRenderer 설계 문서화 및 검증

### 1. D항목 문서 소유권 분리 확정

D항목(SceneRenderer 합성 클래스)의 구현 설계는 `DeferredRenderer_Design.md`가 소유하고,  
`Refactoring_RenderAPI.md`에는 참조자만 남기는 원칙 확정.

- `Refactoring_RenderAPI.md` D항목 수정: 상세 설계 제거 → 참조 링크로 대체
  > `> 구체 설계 및 구현 계획 → DeferredRenderer_Design.md §7`
- 메모리(`feedback_doc_ownership.md`) 저장 — 추후 세션에서도 적용

---

### 2. DeferredRenderer_Design.md 확장

→ [`DeferredRenderer_Design.md`](Plan/DeferredRenderer_Design.md) 참조

주요 추가/수정 항목:

| 섹션 | 내용 요약 |
|---|---|
| §7 (SceneRenderer 합성 클래스) | SceneRenderer 클래스 설계, Execute.cpp 변경 전/후, 변경 파일 목록 전체 추가 |
| §9 (구현 단계 테이블) | 상태 열 추가, Stage 0 행 추가 |
| §11 (구현 진행 현황) | 기존 시스템 역할 변화표, ForwardRenderer 역할 범위 변화, 다음 착수 조건 추가 |
| §12 (단계 0 RHI 확장) | 신규 — 하위 항목 §12.1~§12.8 전체 추가 (아래 참조) |

---

### 3. 단계 0 RHI 확장 항목 도출 (§12)

기존 코드 분석을 통해 Deferred 구현 착수 전에 완료해야 하는 RHI 확장 6종 식별.

| 항목 | 파일 |
|---|---|
| §12.1 `EPixelFormat::R16G16B16A16_FLOAT = 10` | `Common_RHI.h` |
| §12.2 `PipelineStateDesc` MRT (`rtvFormats[8]` + `rtvCount`) | `PipelineDesc.h` |
| §12.3 `IColorBuffer`(신규, RT+SRV) / `ITextureUAV::GetRTV()` 추가 | `IColorBuffer.h`, `ColorBuffer_DirectX.h/.cpp`, `ITextureUAV.h` |
| §12.4 `IRenderDevice::CreateColorBuffer` 팩토리 | `IRenderDevice.h` + DX12 구현 |
| §12.5 `IDepthBuffer::BindSRV()` + typeless 포맷 변경 | `IDepthBuffer.h`, `DepthBuffer_DirectX.h/.cpp` |
| §12.6 `CB_DeferredCamera` ScreenW/ScreenH 필드 | DeferredLightingRenderer 내부 |
| §12.7 HDR Output → 백버퍼 Blit Pass 방식 확정 | `FrameOutput` 구조체 변경 없음 |
| §12.8 CB_Lights 중복 정책 — 각자 독립 업로드 유지 | — |

**기존 지원 확인 (추가 작업 불필요):**
- `RenderPassDesc::renderTargets[8]` ✅
- `BeginRenderPass` MRT (`CommandList_DirectX.cpp:220`) ✅
- RenderGraph 배리어 자동화 ✅

---

### 4. 계획서 종합 검증

추상화 설계, LSP 위반 가능성, 구현 상세 등 계획서 전반 검토.  
발견된 문제점 4건을 `§1-1 검토 필요` 항목으로 정리.

→ [`DeferredRenderer_Design.md §1-1`](Plan/DeferredRenderer_Design.md) 참조

| # | 항목 | 심각도 |
|---|---|---|
| A | §5 G-Buffer RT 타입 `ITextureUAV` → `IColorBuffer`로 수정 필요 (§12.3 불일치) | 중 |
| B | `GBufferRenderer : public IRenderer` 시 Initialize 시그니처 충돌 | 중 |
| C | Depth 소유권 이전 미설계 (ForwardRenderer → GBufferRenderer) | 중 |
| D | `AddOpaquePasses` 내 Sky+Opaque 결합 — SkyPass 분리 메서드 미명시 | 소 |

---

---

## Compact Log #5 — 단계 0 RHI 확장 구현 (§12)

→ [`DeferredRenderer_Design.md §12`](Plan/DeferredRenderer_Design.md) 참조

| 항목 | 작업 내용 |
|---|---|
| §12.1 `EPixelFormat::R16G16B16A16_FLOAT` | `Common_RHI.h` 추가 (사용자 직접 완료) |
| §12.2 `PipelineStateDesc` MRT | `rtvFormat` → `rtvFormats[8]` + `rtvCount = 1`. `PipelineState_DirectX` DX12 측 루프 처리. ForwardRenderer / AnimationRenderer / DebugTextRenderer / DebugLineRenderer PSO 생성 코드 일괄 수정 |
| §12.3 `IColorBuffer` + `ColorBuffer_DirectX` | 인터페이스(`Create` / `GetRTV` / `BindSRV`) 확정. DX12 구현: RT 1-slot RTV heap + 전역 SRV heap |
| §12.3 `ITextureUAV::GetRTV()` | 인터페이스에 `GetRTV()` 추가. `TextureUAV_DirectX`에 RTV 1-slot heap 생성 및 리소스 플래그에 `ALLOW_RENDER_TARGET` 추가 |
| §12.4 `IRenderDevice::CreateColorBuffer` | `IRenderDevice.h` 팩토리 선언. `RenderDevice_DirectX` 구현 추가 |
| §12.5 `IDepthBuffer::BindSRV()` + typeless | `IDepthBuffer.h`에 `BindSRV()` 추가. `DepthBuffer_DirectX` 리소스 포맷 `R24G8_TYPELESS`로 변경, DSV `D24_UNORM_S8_UINT` / SRV `R24_UNORM_X8_TYPELESS` 분리 생성. `Create()` 시그니처 `IRenderDevice*` 방식으로 통일 |

---

## 현재 상태

- `DeferredRenderer_Design.md` §12 단계 0 RHI 확장 전 항목 완료
- **다음 작업**: 단계 1 — G-Buffer RT 생성 및 포맷 검증

---

## End Log

### ToDo

#### Phase 1

1. [ ] Deffered Renderer 구현. Compute Shader를 이용한 하이브리드 방식으로 구현 예정 (`DeferredRenderer_Design.md`, `Refactoring_RenderAPI.md`)
2. [ ] PostProcessing 구현
3. [ ] Normal Mapping 구현
4. [ ] Shadow 시스템 구현
5. [ ] Particle 시스템 구현

#### Phase 2

1. [x] Timer 시스템 구현 (반복, 단일)
    - [ ] bLoop parameter를 두어 연속 동작 가능하게 만들기 
2. [ ] Camera 추가 구현
    1. Camera에 World Object 처럼 별도의 Transform(Transform.h) 을 적용하는 것에 대한 피드백 및 (필요하다면) 수정
    2. Free Camera에서 Input 완전 분리 (현재 고정형 NameKey 사용 안하면 Free Cam 사용 불가)
    3. TopView Camera (Pointer 혹은 Character Zone이 창 Deadzone 진입 시 World 이동)
    4. Transform 위탁 고정식 Camera (FPS)
    5. Camera With Boom (Cam Chase with Easing)
3. [ ] Collider 구현
    1. CapsuleShape 구현
    2. Mesh에서 이용할 Collision Component 구현 (충돌 관련 DebugLine, 수식 존재)