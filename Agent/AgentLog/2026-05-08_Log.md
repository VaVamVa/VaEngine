# 2026-05-08 Log

## Start Log

- CubeRenderer DX12 구현 완료 (이전 세션)
- NDC z축 수정: 정점 z∈[-0.5, 0.5] 오브젝트 공간으로 복구
- 셰이더 경로 CMake 매크로(`DX_SHADER_DIR`) 적용
- 다형성 아키텍처 계획서 작성 (`Plan_Architecture_Polymorphism.md`)

---

## 작업 내역

### MVP 상수 버퍼 구현 (Step 1)

**목표**: 3D 회전 큐브 렌더링 확인

**변경 파일**:

- `Engine/Private/RHI/DirectX/_Shaders/CubeShader.hlsl`
  - `#pragma pack_matrix(row_major)` 추가
  - `cbuffer CB_Transform : register(b0)` — `float4x4 gMVP`
  - VS: `mul(float4(input.pos, 1.0f), gMVP)`

- `Engine/Private/RHI/Agent/DirectX/CubeRenderer_DX12.h`
  - `std::unique_ptr<IGPUBuffer> constantBuffer` 멤버 추가
  - `CreateConstantBuffer()` 메서드 추가
  - `std::chrono::steady_clock::time_point startTime` 추가

- `Engine/Private/RHI/Agent/DirectX/CubeRenderer_DX12.cpp`
  - `<DirectXMath.h>` include 추가
  - `CreateRootSignature`: Root CBV 파라미터 추가 (`InitAsConstantBufferView(0)`, register b0)
  - `CreateMesh`: 정점 z를 NDC 핵(0.1/0.9) → 오브젝트 공간 ±0.5 복구
  - `CreateConstantBuffer`: 256바이트 Upload 버퍼 생성
  - `Draw`: elapsed time → `XMMatrixRotationY` → LookAtLH → PerspectiveFovLH → Upload → `SetGraphicsRootConstantBufferView`

**결과**: ✅ 회전하는 3D 큐브 렌더링 확인

---

## ToDo

- [ ] Step 2: `IConstantBuffer` 인터페이스 분리 (현재 `IGPUBuffer`로 임시 처리)
- [ ] Step 3: `IMesh` 인터페이스 + `MeshPrimitive` 기반 클래스
- [ ] Step 4: `IPipelineState` + `IBindingLayout` (PSO / Root Signature 추상화)
- [ ] Step 5: `ITexture` + Diffuse 텍스처
- [ ] Step 6: `ICamera` (Freedom 카메라)
- [ ] Step 7: `IMaterial` + `ILight` (Phong 조명)
- [ ] Step 8: Depth Buffer
