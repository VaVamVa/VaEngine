# 2026-05-04 Log

---

## 오늘 한 작업

### 1. 아키텍처 리팩터링

| 작업 | 내용 |
|------|------|
| `Engine/Public/Math/MatrixUtil.h` 생성 | Execute.cpp의 `MatUtil` namespace 추출 |
| `Engine/Public/Geometry/CubeMesh.h` 생성 | Execute.cpp의 `CUBE_VERTICES`, `CUBE_INDICES` 추출 |
| `IExecute.h` Engine → Application/Public 이동 | Engine은 앱 라이프사이클 인터페이스를 알 필요 없음 |
| `Execute.h/.cpp` ExecWindows → Application 이동 | 씬 로직은 Application 계층 소속 |
| `Execute.h/.cpp` 내 정리 | MatUtil 제거, CubeMesh 제거, HWND/HINSTANCE 멤버 제거 |
| `Application/CMakeLists.txt` 수정 | Private GLOB 소스 추가, Public 헤더 노출 |
| `ExecWindows/ExecWindows.cpp` 수정 | include 경로 수정 |

### 2. Android Vulkan 구현

| 파일 | 내용 |
|------|------|
| `Engine/Private/RHI/Vulkan/Common_Vulkan.h` | `#ifdef ANDROID` — `vulkan_android.h`, `VK_KHR_ANDROID_SURFACE_EXTENSION_NAME` 추가 |
| `Engine/Private/RHI/Vulkan/SwapChain_Vulkan.cpp` | `CreateSurface()` — `vkCreateAndroidSurfaceKHR` 분기 추가 |
| `Engine/Private/RHI/Vulkan/Pipeline_Vulkan.h/.cpp` | `Initialize()` `assetManager` 파라미터 추가, `LoadSpirvFile()` Android 분기 |
| `Engine/Public/Common_RHI.h` | `NativeDisplayInfo.Display` — Android에서 `AAssetManager*` 역할 명시 |
| `Application/Private/Execute.cpp` | `InitVulkan3DScene()` — Android 빌드 시 `AAssetManager*` 전달 |
| `Platforms/ExecAndroid/Source/main.cpp` | **신규** — `android_native_app_glue` 기반 `android_main` 진입점 |
| `Platforms/ExecAndroid/CMakeLists.txt` | `android_native_app_glue` 추가, 링크 옵션 수정, 셰이더 컴파일 섹션 추가 |
| `Platforms/ExecAndroid/app/src/main/AndroidManifest.xml` | `NativeActivity`로 변경 |

### 3. CMake 빌드 시스템 개선

| 파일 | 변경 내용 |
|------|---------|
| `Engine/CMakeLists.txt` | `find_program(GLSLC_EXECUTABLE)` 위치 이동 (Android Preset에서도 탐색 가능), 탐색 힌트 보강 |
| `VaEngine/CMakeLists.txt` | `android-apk` 커스텀 타겟 추가 — IDE에서 APK 빌드 가능 |
| `Platforms/ExecWindows/CMakeLists.txt` | Windows 빌드 시 Android assets로 `.spv` 자동 복사 추가 |
| `Platforms/ExecAndroid/CMakeLists.txt` | Android Preset 단독으로 셰이더 컴파일 및 assets 배치 가능 |

---

## 현재 프로젝트 상태

- `Windows-Vulkan` 브랜치: 3D 큐브 렌더링 구현 완료, 아키텍처 정리 완료
- `ExecAndroid`: Android Vulkan 진입점 구현 완료, 빌드 및 동작 미확인

---

## 다음 할 일

- [ ] windows-vulkan preset 빌드 및 동작 확인 (리팩터링 후 회귀 여부)
- [ ] Android 기기/에뮬레이터에서 빌드 및 동작 확인 (회전 큐브 출력)
- [ ] RHI 인터페이스 개선 — `Plan_MakeUp.md` 참고
