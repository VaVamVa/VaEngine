> [공지] 아래의 이전 기록들은 "Vulkan Branch"에서 진행된 작업 내역입니다.

# 2026-05-04 Log (Vulkan Branch)

---

## 오늘 한 작업

### 1. 아키텍처 리팩터링

| 작업 | 내용 |
|------|------|
| `Engine/Public/Math/MatrixUtil.h` 생성 | Execute.cpp의 `MatUtil` namespace 추출 |
| `Engine/Public/Geometry/CubeMesh.h` 생성 | Execute.cpp의 `CUBE_VERTICES`, `CUBE_INDICES` 추출 |
| `IExecute.h` Engine → Application/Public 이동 | Engine은 앱 라이프사이클 인터페이스를 알 필요 없음 |
| `Execute.h/.cpp` ExecWindows → Application 이동 | 씬 로직은 Application 계층 소속 |
| `Execute.h/.cpp` 내 정리 | MatUtil 제거, CubeMesh 제거, HWND/HINSTANCE 멤버 제거 |
| `Application/CMakeLists.txt` 수정 | Private GLOB 소스 추가, Public 헤더 노출 |
| `ExecWindows/ExecWindows.cpp` 수정 | include 경로 수정 |

### 2. Android Vulkan 구현

| 파일 | 내용 |
|------|------|
| `Engine/Private/RHI/Vulkan/Common_Vulkan.h` | `#ifdef ANDROID` — `vulkan_android.h`, `VK_KHR_ANDROID_SURFACE_EXTENSION_NAME` 추가 |
| `Engine/Private/RHI/Vulkan/SwapChain_Vulkan.cpp` | `CreateSurface()` — `vkCreateAndroidSurfaceKHR` 분기 추가 |
| `Engine/Private/RHI/Vulkan/Pipeline_Vulkan.h/.cpp` | `Initialize()` `assetManager` 파라미터 추가, `LoadSpirvFile()` Android 분기 |
| `Engine/Public/Common_RHI.h` | `NativeDisplayInfo.Display` — Android에서 `AAssetManager*` 역할 명시 |
| `Application/Private/Execute.cpp` | `InitVulkan3DScene()` — Android 빌드 시 `AAssetManager*` 전달 |
| `Platforms/ExecAndroid/Source/main.cpp` | **신규** — `android_native_app_glue` 기반 `android_main` 진입점 |
| `Platforms/ExecAndroid/CMakeLists.txt` | `android_native_app_glue` 추가, 링크 옵션 수정, 셰이더 컴파일 섹션 추가 |
| `Platforms/ExecAndroid/app/src/main/AndroidManifest.xml` | `NativeActivity`로 변경 |

### 3. CMake 빌드 시스템 개선

| 파일 | 변경 내용 |
|------|---------|
| `Engine/CMakeLists.txt` | `find_program(GLSLC_EXECUTABLE)` 위치 이동 (Android Preset에서도 탐색 가능), 탐색 힌트 보강 |
| `VaEngine/CMakeLists.txt` | `android-apk` 커스텀 타겟 추가 — IDE에서 APK 빌드 가능 |
| `Platforms/ExecWindows/CMakeLists.txt` | Windows 빌드 시 Android assets로 `.spv` 자동 복사 추가 |
| `Platforms/ExecAndroid/CMakeLists.txt` | Android Preset 단독으로 셰이더 컴파일 및 assets 배치 가능 |

---

# Start Log
- 20260504
- 작업자 : VaVamVa

### To Do

1. **아키텍처 리팩터링 (메인 브랜치 적용)**
    - `Execute` 클래스를 `Application` 레이어로 이동하기 위한 구조 설계 및 적용

2. **명령 기록 시스템 구축**
    - `ICommandList`, `ICommandAllocator` 인터페이스 정의
    - `CommandList_DirectX`, `CommandAllocator_DirectX` 구현

3. **첫 렌더링 명령 (Clear)**
    - 백버퍼를 특정 색상으로 Clear하는 명령 기록 및 제출 로직 구현
    - `OnRender` 루프 연결

---

## Compact Log 1
- 20260504 ~ 20260506
- 작업자 : VaVamVa

### 요약 내용

#### 1. Execute 클래스 Application 레이어로 이동 ✅

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `Execute.h/.cpp` 위치 | `Platforms/ExecWindows/Private/` | `Application/Source/Public`, `Private/` |
| 의존성 | ExecWindows에서만 사용 | Engine 공개 인터페이스만 참조 |
| 플랫폼 핸들 | HWND/HINSTANCE 멤버 직접 보유 | `NativeDisplayInfo`로만 수신 |

`OnInitialize` — RHI::CreateRenderDevice → CommandQueue → Fence → SwapChain 순차 생성.
`OnDestroy` — Fence 신호 및 완료 대기 후 리소스 역순 정리 (GPU Flush 확보).

#### 2. RHI 인터페이스 재구조화 ✅

| 변경 전 | 변경 후 |
|---|---|
| `Engine/Public/Interfaces/` | `Engine/Public/RHI/` |
| `Common_RHI.h` (기본 Desc 구조체) | `Common_RHI.h` — EPixelFormat, EResourceState(32비트 플래그), ResourceBarrier 추가 |

- `ICommandAlloc` 인터페이스 신규 추가 (`Register`, `Reset`)
- `ICommandList`에 `SetResourceBarrier`, `ClearRenderTargetView` 추가
- `ISwapChain`에 `GetCurrentBackBufferRTV()` 추가
- `IGPUBuffer.h`, `IRHIResource.h` 예약 파일 생성

#### 3. DX12 명령 기록 시스템 구현 ✅

| 파일 | 주요 구현 |
|---|---|
| `CommandAlloc_DirectX.h/.cpp` | `ID3D12CommandAllocator` 래핑, `Reset()` |
| `CommandList_DirectX.h/.cpp` | `ID3D12GraphicsCommandList` 래핑, `Begin/Close`, `SetResourceBarrier`, `ClearRenderTargetView` |
| `RenderDevice_DirectX` | `CreateCommandAllocator()` 추가 |
| `SwapChain_DirectX` | `GetCurrentBackBufferRTV()` 추가 |

#### 4. CMakePresets.json + RHILoader 개선 ✅

| 항목 | 내용 |
|---|---|
| 매크로 통일 | `USE_DX12` → `USE_DIRECTX` 전체 통일 |
| `engine` 프리셋 신규 | `USE_DEBUGGING=ON` — 두 백엔드 동시 인스턴스화로 인터페이스 구현 검증 |
| `base` hidden 프리셋 | generator/architecture 공통 정의, 전체 프리셋 상속 |
| `RHILoader.cpp` | `USE_DEBUGGING` 블록 — 각 백엔드 독립 `#if`로 확장 가능한 검증 구조 |

**핵심 설계 결정**: `#if`를 `if constexpr`로 완전 대체 불가 (CMake 플래그를 소비하는 진입점이 반드시 전처리기여야 함). `USE_DEBUGGING`으로 IDE에서 모든 백엔드 분기를 활성화하는 절충안 채택.

---

## End Log
- 20260506
- 작업자 : VaVamVa

### 세션 종료 상태

#### 빌드 상태
| 프리셋 | CMake 구성 | 컴파일 |
|---|---|---|
| windows-directx | ✅ 성공 | ✅ 성공 |
| windows-vulkan | ✅ 성공 | ✅ 성공(other branch) |
| android | 미확인 | 미확인 |
| engine | ✅ 성공 | 미확인 |

#### 작업 완료 항목
- Execute 클래스 Application 레이어 이동 완료
- RHI 인터페이스 `Engine/Public/RHI/`로 재구조화 완료
- ICommandAlloc 인터페이스 + CommandAlloc_DirectX 구현 완료
- CommandList_DirectX 구현 완료 (ResourceBarrier, ClearRenderTargetView)
- Common_RHI.h 확장 (EResourceState 16비트 플래그, ResourceBarrier)
- CMakePresets.json engine 프리셋 추가 및 base 프리셋 분리
- USE_DIRECTX 매크로 통일 및 RHILoader USE_DEBUGGING 구조 완성

#### 미완료 항목 (다음 세션 시작 항목)
1. **첫 렌더링 명령 (Clear)**
    - `OnRender`에서 백버퍼를 단색으로 Clear하는 명령 기록 및 제출
    - `CommandAllocator` 리셋 → `CommandList Begin` → Barrier(Present→RenderTarget) → ClearRTV → Barrier(RenderTarget→Present) → `Close` → `ExecuteCommandLists` 흐름
2. **WaitForPreviousFrame 완성**
    - 프레임 간 Fence 기반 GPU 동기화 루프 구현
    - `Signal → WaitForSingleObjectEx → 펜스값 증가` 패턴