# VaEngine 2026-05-06

## 작업 요약
- **첫 렌더링 구현**: `Execute::OnRender`에서 백버퍼 Clear 명령 기록 및 실행 성공.
- **동기화 로직**: `IFence`를 이용한 CPU-GPU 동기화 루프(`currentFenceValue` 기반) 구현 완료.
- **RHI 추상화 확장**: `IResourceView` 인터페이스 도입 및 `ResourceView_DirectX` 구현으로 렌더 타겟 관리 개선.
- **메모리 안정화**: `TRHIResource`에 커스텀 삭제자를 적용하여 종료 시 COM 객체 해제(Release) 오류 해결.

## 빌드 상태
| 프리셋 | CMake 구성 | 컴파일 | 실행 |
|---|---|---|---|
| windows-directx | ✅ 성공 | ✅ 성공 | ✅ 성공 (Clear Color 출력) |

## To Do (다음 작업)
1. **버텍스 버퍼(Vertex Buffer) 구현**
    - `IBuffer` 인터페이스 설계 및 DX12 구현체 작성.
    - 정점 데이터를 GPU에 업로드하는 로직 추가.
2. **파이프라인 상태 객체(PSO) 설계**
    - `IPipelineState` 인터페이스 및 셰이더 로딩 로직 준비.
3. **첫 삼각형 그리기**
    - `DrawInstanced` 명령 추가 및 화면 출력 확인.
